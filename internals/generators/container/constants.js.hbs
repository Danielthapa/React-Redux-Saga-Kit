/*
 *
 * {{ properCase name }} constants
 *
 */

const constantKey = 'app/containers/{{ properCase name}}';

const {{ camelCase name }}Constants = {
  DEFAULT_ACTION: `${constantKey}/DEFAULT_ACTION`,
  DEFAULT_ACTION_WITH_PAYLOAD: `${constantKey}/DEFAULT_ACTION_WITH_PAYLOAD`,
};

export default {{ camelCase name }}Constants;